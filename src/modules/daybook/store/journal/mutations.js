
export const setEntries = (state) => { //Estas mutaciones tienen que ser sincronas
    state.count++;
    state.lastMutation = 'increment'
}

export const updateEntries = (state) => { //Estas mutaciones tienen que ser sincronas
    state.count++;
    state.lastMutation = 'increment'
}

export const addEntry = (state) => { //Estas mutaciones tienen que ser sincronas
    state.count++;
    state.lastMutation = 'increment'
}
